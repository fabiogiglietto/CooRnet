% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_ctshares.R
\name{get_ctshares}
\alias{get_ctshares}
\title{get_ctshares}
\usage{
get_ctshares(
  urls,
  url_column,
  date_column,
  platforms = "facebook,instagram",
  nmax = 500,
  sleep_time = 20,
  clean_urls = FALSE
)
}
\arguments{
\item{urls}{a dataframe with at least a column "url" containing the URLs, and a column "date" with their published date}

\item{url_column}{name of the column (placed inside quote marks) where the URLs are stored}

\item{date_column}{name of the column (placed inside quote marks) where the date of the URLs are stored}

\item{nmax}{max number of results for query (default 500 as per API limit)}

\item{sleep_time}{pause between queries to respect API rate limits. Default to 20 secs, it can be lowered or increased depending on the assigned API rate limit}

\item{clean_urls}{clean the URLs from tracking parameters (default FALSE)}
}
\value{
A tibble (tbl_df) containing the entities that shared the URLs and a number of variables returned by the \href{https://github.com/CrowdTangle/API/wiki/Links}{CrowdTangle API links endpoint}
}
\description{
A function to get the URLs shares from CrowdTangle
}
\details{
To start using the library you need to set the CrowdTangle API key.
  Open the environment variable file with file.edit("~/.Renviron"), write CROWDTANGLE_API_KEY = <YOUR_API_KEY>, save the file and restart your current R session to start using the CrowdTangle API
}
\examples{
  df <- get_ctshares(urls, url_column=â€œurlâ€, date_column=â€œdateâ€, platforms="facebook,instagram", nmax=100, sleep_time=20, clean_urls=FALSE)

}
